import React from 'react'

const Header = () => {
    return (
        <h2 className="ui center aligned icon header">
            <i className="circular clock icon"></i>
                Pomodoro Timer
        </h2>
    )
}

export default Header